<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>

  <%= f.submit "Allé ça part!", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back to restaurants", restaurants_path %>
